import {createServer} from 'node:http';import {readFile} from 'node:fs/promises';import {extname,join} from 'node:path';
const root=new URL('../dist/lost-found-web/browser/',import.meta.url).pathname;const types={'.js':'text/javascript','.css':'text/css','.svg':'image/svg+xml','.ico':'image/x-icon'};createServer(async(req,res)=>{try{const path=req.url==='/'?'index.html':req.url.slice(1);const file=path.includes('.')?path:'index.html';const data=await readFile(join(root,file));res.setHeader('content-type',types[extname(file)]||'text/html');res.end(data)}catch{res.statusCode=404;res.end()}}).listen(4300,'127.0.0.1');
